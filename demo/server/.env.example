# Copy to server/.env — this is where you set DATABASE CREDENTIALS.
# Never commit real passwords.

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=your_database_name
DB_TYPE=postgres

# For MySQL use:
# DB_TYPE=mysql
# DB_PORT=3306
